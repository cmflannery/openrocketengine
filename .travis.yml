sudo: false
language: python
python:
  - "3.5"
  - "3.6"
  - "nightly" # currently points to 3.7 dev branch. Will migrate to 3.7 once available.
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: pytest
